dependencies {
	compile project(":modules:contacts:contacts-api")

	compileOnly group: "com.liferay.portal", name: "release.dxp.api"
}

buildService {
	apiDir = "../contacts-api/src/main/java"
}

group = "com.crosig.contacts"